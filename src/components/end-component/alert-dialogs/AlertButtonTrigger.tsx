import React, { FC } from 'react';

type AlertButtonTrigger = {
	children: React.ReactNode;
};

const AlertButtonTrigger: FC<AlertButtonTrigger> = ({ children }) => {
	// api

	// hooks

	// states

	// variables

	// styles

	// functions

	// effects

	// components

	return (
		<p className='w-full relative flex cursor-default select-none items-center rounded-sm px-2 py-1.5 text-sm outline-none transition-colors hover:bg-accent focus:bg-accent focus:text-accent-foreground data-[disabled]:pointer-events-none data-[disabled]:opacity-50"'>
			{children}
		</p>
	);
};

export default AlertButtonTrigger;
