import React, { FC } from 'react';

type FormRightRowsProps = {
	children: React.ReactNode;
};

const FormRightRows: FC<FormRightRowsProps> = ({ children }) => {
	// api

	// hooks

	// states

	// variables

	// styles

	// functions

	// effects

	// components

	return <div className='grid auto-rows-max items-start gap-4 lg:gap-8'>{children}</div>;
};

export default FormRightRows;
