import React, { FC } from 'react';

type FormLeftRowsProps = {
	children: React.ReactNode;
};

const FormLeftRows: FC<FormLeftRowsProps> = ({ children }) => {
	// api

	// hooks

	// states

	// variables

	// styles

	// functions

	// effects

	// components

	return (
		<div className='grid auto-rows-max items-start gap-4 lg:col-span-2 lg:gap-8'>{children}</div>
	);
};

export default FormLeftRows;
